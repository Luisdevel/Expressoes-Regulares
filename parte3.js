const { texto } = require('./base');

// * (opcionais) 0 ou n
// + (obrigatório) 1 ou n
// ? (opcionais) 0 ou 1

console.log(texto);

// const regExpt1 = /Jo+ão+/gi;

console.log(texto.match(regExpt1));